# Core data/science libs (pinned for stability)
numpy~=1.26.4  # Avoids dtype errors; compatible with pandas/sklearn
pandas~=2.2.2
matplotlib~=3.9.0
seaborn~=0.13.2
plotly~=5.22.0
scipy~=1.14.1

# ML basics
scikit-learn~=1.5.2  # Latest; fixes gplearn compat (see code tweak below)
joblib~=1.4.2

# Advanced ML
tensorflow~=2.17.0  # Updated; compatible with numpy 1.26
shap~=0.46.0

# Optimization/GA
deap~=1.4.1
scikit-optimize~=0.10.2

# UI/Logging
streamlit~=1.38.0
rich~=13.7.1
structlog~=24.2.0
pydantic~=2.8.0

# Excel/IO
openpyxl~=3.1.5

# Formula Discovery (gplearn only for cloud; no pysr)
gplearn~=0.4.2
sympy~=1.13.1

# Optional: If using Jupyter locally
# jupyter~=1.0.0
